{
  "name": "geekslab-ecosystem",
  "private": true,
  "packageManager": "npm@10.4.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "prettier": "^3.0.0",
    "turbo": "latest"
  },
  "prettier": "@geekslab/config/prettier-preset.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
